import React from "react";
import { AccountBackground, AccountCover } from "../styles";

export const AccountScreen = () => {
  return (
    <AccountBackground>
      <AccountCover />
    </AccountBackground>
  );
};
